Code tested on Python version 3.8, however should work for other common versions as well.

Linear regression code is in the linear_regression/ folder with two functions lr_fit (to fit data to model) and lr_predict.
The other type of regression model chosen is polynomial regression with regularization which is implemented as a class PolynomialRegression in the folder polynomial_regression